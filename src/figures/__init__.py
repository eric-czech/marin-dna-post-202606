"""Figure set for the transfer-validation / parameter-scaling write-up.

Each `figureN_*` module exposes a `build(...)` function that renders and saves
one figure. `python -m figures` (or running `__main__.py`) rebuilds them all.
"""
